<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.comento.oracleSpringBoot.mapper.SongMapper">
	<select id="select" resultType="com.comento.oracleSpringBoot.dto.song.Song">SELECT name, songBy, n FROM Song</select>

    <insert id="insertPlay" parameterType="map">INSERT into song_play (stage, song, play_date) VALUES (#{stage}, #{song}, #{playDate})</insert>

    <update id="update">UPDATE Song SET name = #{dto.name}, songBy = #{dto.songBy} WHERE n = #{n}</update>

    <insert id="insert" parameterType="com.comento.oracleSpringBoot.dto.song.Song">INSERT INTO Song (name, songBy) VALUES (#{name}, #{songBy})</insert>

    <select id="selectPlay" resultType="map">
        SELECT s.name songName, st.name stage, sp.play_date playDate
        FROM song_play sp
        JOIN Song s ON sp.song = s.n
        JOIN song_stage st ON sp.stage = st.n
        <where>
            <if test="from != null and from != ''"> AND sp.play_date &gt;= #{from}</if>
            <if test="to != null and to != ''"> AND sp.play_date &lt;= #{to}</if>
            <if test="stage != null"> AND sp.stage = #{stage}</if>
        </where>
        ORDER BY sp.play_date DESC
    </select>
</mapper>