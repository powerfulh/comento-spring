<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.comento.oracleSpringBoot.mapper.AdminMapper">
    <select id="selectUserLog" resultType="ReqLog">
        select l.n, l.`method`, l.url, l.param, l.insert_id insertId, l.insert_date insertDate from user_log l
        order by l.n desc limit 20
    </select>
</mapper>